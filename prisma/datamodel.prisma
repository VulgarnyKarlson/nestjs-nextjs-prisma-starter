type Todo {
  id: ID! @unique
  content: String!
  completed: Boolean!
}

type TodoAction {
  id: ID! @unique
  type: String!
  context: String
}

type User {
  id: ID! @unique
  email: String! @unique
  firstName: String!
  lastName: String!
  passwordDigest: String!
}
